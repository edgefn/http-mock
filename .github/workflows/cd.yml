name: CD Mock service

on:
  workflow_run:
    workflow: [ "Release", "CI" ]
    types:
      - completed

jobs:
  deploy:
    runs-on:
      group: mock

    steps:
      - name: deploy
        run: |
          sudo systemctl restart http-mock
          echo "deploy done"
