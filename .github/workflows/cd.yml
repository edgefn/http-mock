name: CD Mock service

on:
  workflow_call:

jobs:
  deploy:
    runs-on:
      group: mock

    steps:
      - name: deploy
        run: |
          sudo systemctl restart http-mock
          echo "deploy done for test"
